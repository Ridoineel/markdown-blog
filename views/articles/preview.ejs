<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../components/head") %>
    
    <title>Article Preview</title>
</head>
<body>
    <div class="container pt-3">

        <div class="p-3  article-header shadow-3d-sm bg-fade">
            <a href="/articles">
                <button class="btn prewious-btn" style="font-size: 1.5em">
                    &LeftTeeArrow;
                </button>
            </a>
            <h1><%= article.title %></h1>
            
            <div class="text-muted">

                <span class="font-weight-bold">
                    <%= article.author %>
                </span> <br>
                <%= article.createAt.toLocaleString() %>
            </div>

            <a href="/articles/edit/<%= article._id %>">
                <button class="btn btn-primary edit-btn">
                    Edit 
                </button>
            </a>
        </div>

        <div class="border border-sm p-2 shadow-sm">
            <%- article.contentHtml %>
        </div>
    </div>
</body>
</html>